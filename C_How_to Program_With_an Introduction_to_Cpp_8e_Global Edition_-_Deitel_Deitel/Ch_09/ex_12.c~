#include <stdio.h>

int main(void)
{
  printf("%s\n?%s\n", "\? with an escape character: "," has printed successfully.");
  printf("%s\n\?%s\n", "\? without an escape character: ", " has printed successfully.");  
}
