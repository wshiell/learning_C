#include <stdio.h>

int main(void)
{
  int multiples = 1, sum = 0;

  for ( unsigned int counter = 7; counter <= 100; counter += 7) {
    multiples = counter * counter;
    sum += multiples;
  }

  printf("Sum of all multiples of 7 from 1 to 100 is: %d\n", sum);
}
