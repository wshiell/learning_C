#include <stdio.h>
#include <math.h>
#include <string.h>

int main(void)
{
  int i;
  int j;
  int k;
  str = "a";
  
  puts("Enter things:");
  k = scanf("%d%d", &i, &j);
  printf("i^j = %f\n", pow(i, j));
  printf("k = %d\n", k);
}
