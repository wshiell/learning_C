#include <stdio.h>
#define SIZE 3

int main(void)
{
  int ASCII;
  
  puts("Enter the ASCII code for a character (type '-1' to quit):");
  scanf("%d", &ASCII);

  while (ASCII != -1) {

    printf("\%s%d\n%s%c\n", "ASCII Code: ", ASCII, "Character: ", ASCII);
    puts("Enter the ASCII code for a character (type '-1' to quit):");
    scanf("%d", &ASCII);
  }
}
