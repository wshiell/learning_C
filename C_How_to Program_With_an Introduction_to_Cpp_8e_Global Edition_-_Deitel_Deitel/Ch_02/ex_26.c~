#include <stdio.h>

int main ( void )
{

  int a, b;

  puts( "Enter two integers: " );
  scanf( "%d %d", &a, &b );

  if ( a % b == 0 ) {
    printf( "%d is a multiple of %d.", a, b );
  }
  if ( b % a == 0 ) {
    printf( "%d is a multiple of %d.", b, a );
  }
}
