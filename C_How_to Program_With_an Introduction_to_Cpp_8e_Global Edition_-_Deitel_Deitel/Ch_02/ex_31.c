#include <stdio.h>

int main ( void )
{

  int num;
  int square, cube;

  square = num * num;
  cube = num * num * num;

  puts( "Number\tSquare\tCube" );


  num = 0;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );
  
  num = 1;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 2;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 3;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 4;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 5;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 6;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 7;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 8;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 9;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

  num = 10;
  square = num * num;
  cube = num * num * num;
  printf ( "%d\t%d\t%d\n", num, square, cube );

}
