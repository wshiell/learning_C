1. a) input redirect operator, <
   b) output redirect operastor, >
   c) apend operator, >>
   d) pipe, |
   e) ellipsis, ...
   f) va_list XX WRONG - va_start
   g) va_arg
   h) va_end
   i) argc
   j) argv
   k) make, Makefile
   l) exit
   m) atexit
   n) constant- XX WRONG - suffix
   o) signal
   p) raise
   q) calloc
   r) realloc