#include <stdio.h>

int main(void)
{
  for (unsigned int counter = 1; counter <=10; counter++) {
    for (unsigned int tally = 1; tally <= counter; tally++) {
      printf("%s", "*");
    }
    puts("");
  }
}
